<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DiscordTheme" parent="Theme.AppCompat" >
        <item name="colorPrimary">@color/primary_discord</item>
        <item name="colorAccent">@color/accent_discord</item>
        <item name="colorPrimaryDark">@color/primary_discord_dark</item>
    </style>

</resources>
